# 2026-09-05 — AUTH-002: вход пользователя (backend)

## Статус: выполнено

## Что сделано

- Добавлена зависимость `PyJWT==2.10.1` (`requirements.txt`).
- В `app/core/config.py` добавлены настройки cookie: `ACCESS_TOKEN_COOKIE_NAME`, `COOKIE_SECURE` (по умолчанию false, включать только за HTTPS). В `.env.example` — комментарий про `COOKIE_SECURE`.
- В `app/core/constants.py` добавлено сообщение `MSG_INVALID_CREDENTIALS` (единый текст для неверного логина и пароля, чтобы не раскрывать наличие аккаунта).
- В `app/services/auth.py` добавлена `create_access_token(user)` — JWT с `sub` (id пользователя), `role`, `iat`, `exp` на основе `settings.JWT_EXPIRE_MINUTES`.
- В `app/schemas/auth.py` добавлены `LoginRequest` (нормализация логина как при регистрации, пароль только непустой) и `LoginResponse` (тот же состав, что и у `RegisterResponse`).
- В `app/routers/auth.py` добавлен `POST /auth/login`: поиск пользователя по логину, проверка пароля, выдача JWT в httpOnly cookie (samesite=lax, path=/, max_age = сутки), ответ — данные пользователя.

## Файлы

| Файл | Действие |
|---|---|
| `backend/requirements.txt` | изменён (+PyJWT) |
| `backend/.env.example` | изменён (+COOKIE_SECURE) |
| `backend/app/core/config.py` | изменён (+cookie-настройки) |
| `backend/app/core/constants.py` | изменён (+MSG_INVALID_CREDENTIALS) |
| `backend/app/services/auth.py` | изменён (+create_access_token) |
| `backend/app/schemas/auth.py` | изменён (+LoginRequest/LoginResponse) |
| `backend/app/routers/auth.py` | изменён (+POST /auth/login) |
| `backend/tests/test_auth.py` | изменён (+тесты логина) |
| `openspec/TASKS.md` | изменён (AUTH-002 -> done) |

## Затронутые тесты

`pytest tests/` — 37 passed. Добавлены: успешный вход с httpOnly cookie, валидность JWT (sub/role/ttl), нормализация логина, неверный пароль, неизвестный логин, пустые поля; юнит-тест `create_access_token`.

## Что осталось / передаётся дальше

- Следующая задача по Этапу 2 — AUTH-003 (вход для сотрудников по коду) и AUTH-007 (защита приватных маршрутов, проверка JWT из cookie).
- Фронтенд `Login.jsx` всё ещё показывает заглушку "бэкенд ещё не реализован" (`serverNotReady`) — после AUTH-007 его нужно подключить к `/auth/login`.

## Дополнение: сквозная проверка в Docker + баг `.dockerignore`

Выполнен сквозной сценарий в Docker (`docker compose up --build -d`):

- `GET /health` → `{"status":"ok","database":"connected"}`.
- `POST /auth/register` → **201**.
- `POST /auth/login` → **200** + `Set-Cookie: access_token=...; HttpOnly`, TTL ровно 24 ч.
- неверный пароль → **401** `{"detail":"Неверный логин или пароль"}`.

Найден и исправлен баг: в `backend/.dockerignore` было исключено `alembic/versions` — файлы миграций не попадали в Docker-образ, поэтому `alembic upgrade head` в entrypoint не находил скриптов и контейнерная БД оставалась без таблиц (запросы падали с `relation "users" does not exist`). Из `.dockerignore` строка `alembic/versions` удалена, backend пересобран. После пересборки `alembic current` → `0003 (head)`.

Передаётся дальше: локальный uvicorn на Windows подключался к локальному Windows-PostgreSQL (PID 7312), занимающему `5432` вместе с контейнером — при разработке вне Docker не забыть про этот конфликт либо переназначить порт.

---

# 2026-09-05 — AUTH-003: вход для сотрудников (backend)

## Статус: выполнено

## Что сделано

- В `app/core/constants.py` добавлены `STAFF_LOGIN_CODE = "123456"` и сообщения `MSG_INVALID_STAFF_CODE`, `MSG_NOT_EMPLOYEE`.
- В `app/schemas/auth.py` общая валидация логина/пароля вынесена в базовый класс `_CredentialsBase` (используется `LoginRequest`), добавлен `EmployeeLoginRequest` (login + code + password, код только непустой).
- В `app/routers/auth.py`:
  - выдача JWT-куки вынесена в переиспользуемый `_set_auth_cookie(response, user)` — используется обоими эндпоинтами входа;
  - добавлен `POST /auth/login/employee`: проверка кода (первой, до запросов БД), затем логин/пароль (единый ответ 401 как в обычном входе), затем проверка роли (403 для не-сотрудников), выдача httpOnly cookie.
  - в `POST /auth/register` добавлен bootstrap: если в БД нет ни одного пользователя (`count() == 0`), первый зарегистрированный получает роль `employee` — иначе `user`.

## Файлы

| Файл | Действие |
|---|---|
| `backend/app/core/constants.py` | изменён (+STAFF_LOGIN_CODE, сообщения) |
| `backend/app/schemas/auth.py` | изменён (+_CredentialsBase, EmployeeLoginRequest) |
| `backend/app/routers/auth.py` | изменён (+POST /auth/login/employee, _set_auth_cookie, bootstrap в register) |
| `backend/tests/test_auth.py` | изменён (+bootstrap и employee-login тесты) |
| `openspec/TASKS.md` | изменён (AUTH-003 -> done) |

## Затронутые тесты

`pytest tests/` — 49 passed (было 37). Добавлено 12: bootstrap (первый регистратор = employee, при наличии пользователей = user), employee login (успех с cookie, JWT с role=employee, обрезка логина, неверный код 401, неверный пароль 401, неизвестный логин 401, несотрудник 403, код проверяется до БД, пустые код/пароль 422).

## Сквозная проверка в Docker

Данные сброшены (`TRUNCATE users RESTART IDENTITY CASCADE`), backend пересобран:

- первый `POST /auth/register` → **201**, `role:"employee"` (bootstrap).
- `POST /auth/login/employee` (anna, 123456) → **200** + httpOnly cookie, JWT `role:"employee"`, TTL 24ч.
- второй `POST /auth/register` → **201**, `role:"user"`.
- `POST /auth/login/employee` для обычного пользователя → **403** «Вход разрешён только сотрудникам».
- неверный код → **401** «Неверный код».

## Что осталось / передаётся дальше

- Следующая задача по Этапу 2 — AUTH-007 (защита приватных маршрутов: middleware на бэкенде + route guard на фронтенде).
- Фронтенд `LoginWork.jsx` (`/loginWork`) ещё не подключён к `POST /auth/login/employee` (заглушка `serverNotReady`).

---

# 2026-09-05 — APP-001: форма новой заявки (frontend `/user/new`)

## Статус: выполнено (с заглушкой на APP-002)

## Что сделано

- В `frontend/src/constants/auth.js` вынесены общие хелперы `TELEGRAM` (`isValid`, `format`) — вычищено дублирование из `Registration.jsx`.
- В `frontend/src/components/Registration.jsx` убраны локальные `telegramIsValid`/`formatTelegram`, используются импортированные `TELEGRAM.*`.
- В `frontend/src/locales/ru.json` добавлен namespace `userNew`.
- Созданы `frontend/src/components/UserNew.jsx` + `UserNew.css`: по текстовому описанию — заголовок «Новая заявка», числовое поле «Сумма», textarea «Причина» (4 строки), ряд из двух колонок «Телеграм» (необязательно) и «Актуальный телеграм канал» (обязательно), поле загрузки файла «Выписка из банка» со скрепкой справа, чекбокс согласия (без него кнопка disabled), кнопка «Запросить» на всю ширину.
- Стили переиспользуют систему с auth-страниц (login/registration): те же CSS-переменные, поля 48px с border #1C201D и радиусом 12px, фокус accent-hover, кнопка-акцент 52px, error-цвет.
- Кнопка «Запросить» при валидной форме показывает заглушку `userNew.serverNotReady` (бэкенд APP-002 ещё не реализован) — по паттерну существующих заглушек Login/Registration.
- Сайдбар COMP-001 не создавался заново — подключён через существующий `Layout`.
- В `frontend/src/App.jsx` маршрут `/user/new` переведён с `PlaceholderPage` на `UserNew`.
- `openspec/TASKS.md`: APP-001 -> done, в APP-002 добавлена пометка о заглушке во фронтенде.

## Файлы

| Файл | Действие |
|---|---|
| `frontend/src/constants/auth.js` | изменён (+TELEGRAM) |
| `frontend/src/components/Registration.jsx` | изменён (переиспользует TELEGRAM) |
| `frontend/src/locales/ru.json` | изменён (+userNew) |
| `frontend/src/components/UserNew.jsx` | создан |
| `frontend/src/components/UserNew.css` | создан |
| `frontend/src/App.jsx` | изменён (маршрут /user/new) |
| `openspec/TASKS.md` | изменён (APP-001 -> done) |

## Затронутые тесты / проверка

- Фронтенд без unit-тестов; проверка — `npm run build` в `frontend/`.

## Что осталось / передаётся дальше

- APP-002 (backend): реализовать `POST` создания заявки и подключить к нему `UserNew.jsx` вместо заглушки `serverNotReady`.
- После APP-002 — STMT-001 (парсинг выписки) и далее Этап 3.

---

# 2026-09-05 — APP-001: доработки формы /user/new (валидация суммы, поле пароля, ротация плейсхолдера)

## Статус: выполнено

## Что сделано

- Создан `frontend/src/constants/application.js` с `AMOUNT = { MIN: 1000, MAX: 10000000 }` (RULES: константы не в бизнес-логике).
- `UserNew.jsx`: валидация суммы по `AMOUNT.MIN/MAX` (атрибуты `min`/`max` на инпуте тоже). Сообщение об ошибке осталось `userNew.amountHint` — «Укажите сумму больше нуля» (существующий ключ, новый не заводился).
- Поле «Пароль» рядом с «Запросить» в `.usernew-actions` (flex-ряд): верификация личности, валидация только «не пустое» (корректность — на бэкенде при APP-002), глазик show/hide — копия паттерна `/login` и `/registration`, aria-ключи через i18n. Без пароля кнопка disabled.
- Ротация плейсхолдера «Причины»: 7 готовых коротких плейсхолдеров (`reasonPlaceholder1..7`, примеры целей кредита в разговорном тоне; прежний текст-инструкция для разработчика удалён полностью), случайный выбор индекса в `useState`-инициализаторе — только при монтировании, не при ре-рендерах (обновляется только при новой загрузке страницы).
- Вёрстка и валидация телеграм-канала не тронуты; password/кнопка в один ряд.

## Файлы

| Файл | Действие |
|---|---|
| `frontend/src/constants/application.js` | создан (+AMOUNT) |
| `frontend/src/components/UserNew.jsx` | изменён (сумма, пароль, ротация плейсхолдера) |
| `frontend/src/components/UserNew.css` | изменён (.usernew-actions, .usernew-field__wrap/__toggle, медиа-стек) |
| `frontend/src/locales/ru.json` | изменён (+password/showPassword/hidePassword, reasonPlaceholder1..7 вместо прежнего текста-инструкции) |

## Проверка

- `npm run build` — успешно (1880 modules).
- Playwright (preview): min/max атрибуты 1000/10000000; сумма 999 → disabled + ошибка «Укажите сумму больше нуля»; 1000 → enabled; 10000001 → disabled; без пароля → disabled; тоггл пароля `password → text`; пароль и кнопка в одном ряду (кнопка справа); за 12 загрузок встречено 4 из 5 плейсхолдеров (случайный выбор).

## Что осталось

- Прежний блокер: APP-002 (backend) + подключение формы вместо заглушки `serverNotReady`.