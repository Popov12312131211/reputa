# 2026-09-04 — COMP-001: переиспользуемый компонент сайдбара

## Статус: выполнено

## Что сделано

- Установлен `lucide-react` (пакет из задания, единственная новая зависимость).
- Добавлен неймспейс `sidebar` в `frontend/src/locales/ru.json` (ключи пунктов меню).
- Создан `frontend/src/contexts/AuthContext.jsx` — роль определяется из URL-пути (`/user/*` -> user, `/employee/*` -> employee). Заглушка до появления JWT-auth, forward-compatible.
- Создан `frontend/src/components/Sidebar.jsx` + `Sidebar.css`:
  - Один переиспользуемый компонент для обеих ролей, набор пунктов формируется по роли из контекста.
  - Иконки из `lucide-react`: Settings / FileText / Pencil (user), Settings / Clock / LayoutList (employee).
  - Ширина по умолчанию 240px, регулируется перетаскиванием (невидимая зона захвата на правом крае, визуальная «ручка» — `border-right`), от 60px до 30vw.
  - Collapse/expand кнопкой: в свёрнутом состоянии остаются только иконки (60px), текст скрыт, tooltip на hover, логотип остаётся, зона захвата скрыта.
  - Активный пункт — акцентный цвет `--accent` и фон `--accent-bg`.
  - Тексты через i18n-ключи.
- Создан `frontend/src/components/Layout.jsx` + `Layout.css` — обёртка `<Sidebar /> + <Outlet />`, flex, контент адаптируется к ширине сайдбара.
- Обновлён `frontend/src/App.jsx` — все `/user/*` и `/employee/*` маршруты вложены под `Layout` с `AuthProvider`, дублирования нет.

## Файлы

| Файл | Действие |
|---|---|
| `frontend/package.json` | изменён (+lucide-react) |
| `frontend/package-lock.json` | изменён |
| `frontend/src/locales/ru.json` | изменён (+sidebar) |
| `frontend/src/contexts/AuthContext.jsx` | создан |
| `frontend/src/components/Sidebar.jsx` | создан |
| `frontend/src/components/Sidebar.css` | создан |
| `frontend/src/components/Layout.jsx` | создан |
| `frontend/src/components/Layout.css` | создан |
| `frontend/src/App.jsx` | изменён |
| `openspec/TASKS.md` | изменён (COMP-001 -> done) |

## Затронутые тесты

Отдельных тестов для фронтенда в проекте не заведено (скриптов тестов в `package.json` нет). Проверена сборка: `npm run build` в `frontend/` — проходит без ошибок.

## Что осталось/передаётся дальше

- Когда будет реализована бэкендная аутентификация (AUTH-001..007), обновить `AuthProvider` для чтения роли из JWT/API вместо URL-пути — компоненты-потребители менять не нужно.
- В задании также упомянуты детальные маршруты с query-параметром `?menu={id}` (`/user/my?menu={id}`, `/employee/application?menu={id}`) — текущий сайдбар навигацию по ним не выделяет отдельно; это выйдет с реализацией APP-005/EMP-005.