# AUTH-009 — Принудительная сессия + запрет входа сотрудника через обычный вход

## Статус
Не начато.

## Описание
Задача добавлена в TASKS.md как бэклог. Реализация не начиналась.

Что предстоит сделать:
- Убедиться, что при истечении/невалидности JWT-куки посреди работы в приложении (не только при первом заходе) пользователя принудительно редиректит на /login. Проверить на реальном сценарии (вручную протухшим/некорректным токеном), а не только на проверке при монтировании страницы.
- Реализовать ограничение: аккаунт с ролью "сотрудник" не может логиниться через обычный /login (без кода). Принять и зафиксировать решение (отклонять попытку входа на эндпоинте /login либо иной подход) — вход для сотрудника разрешён только через /loginWork.

## Затронутые файлы
- Пока нет (задача не начата).

## Проверка
- Пока не проводилась.

## Что осталось
- Вся реализация.

## Блокеры
- Нет.