# Проверка APP-002 — Эндпоинт создания заявки (backend)

## Дата
2026-09-05

## Статус
Подтверждена выполненной (реализация была в ветке `feat/app-002`, незакоммичена).

## Что сделано
- Проверено, что реализация соответствует спецификации: `POST /applications` (multipart/form-data) принимает `amount`, `purpose`, `telegram`, `telegram_channel` и файл выписки `statement`, создаёт `Application` со статусом `in_queue` и `score=None`, привязка к пользователю через JWT из httpOnly cookie (`get_current_user`).
- Файл выписки только принимается и отбрасывается — не сохраняется (парсинг — STMT-001).
- Схемы валидации, константы, JWT-хелперы, зависимость авторизации — на месте.

## Затронутые файлы (проверены, изменений не вносилось)
- backend/app/routers/applications.py
- backend/app/schemas/application.py
- backend/app/api/deps.py
- backend/app/services/security.py
- backend/app/core/constants.py
- backend/app/main.py
- backend/requirements.txt
- backend/tests/test_applications.py
- backend/tests/test_security.py

## Проверка
- `python -m pytest tests -q` в `backend/` → **53 passed** (1 warning — DeprecationWarning в starlette/anyio, не связан с кодом).

## Что осталось
- Подключение фронтенда (APP-001) к этому эндпоинту.
- Парсинг выписки — STMT-001; автообработка по порогам — APP-003; выдача JWT на входе — AUTH-002; защита приватных маршрутов на базе `get_current_user` — AUTH-007.

## Блокеры
- Нет.