# Рефакторинг AUTH-007 — защита приватных маршрутов

## Дата
2026-09-05

## Что сделано
- Упростил `get_current_user`: один helper для всех ответов 401, поведение API не менялось.
- Уточнил границу middleware: защищает `/user` и `/employee` вместе с их подмаршрутами, но пропускает похожие пути.
- Исправил guard: гости `/employee/*` идут на `/loginWork`; неизвестная роль идёт на `/login` без цикла редиректов.
- Добавил регрессионные тесты границы приватных префиксов backend.

## Затронутые файлы
- backend/app/routers/deps.py
- backend/app/main.py
- backend/tests/test_auth_007.py
- frontend/src/components/RequireAuth.jsx
- openspec/tasks/AUTH-007.md

## Проверка
- `python -m pytest` в `backend`: 43 passed.
- Сборка фронтенда не запускалась: тестовой инфраструктуры нет, зависимости ранее не проверялись в этой среде.

## Что осталось
- Сквозная проверка после AUTH-002/AUTH-003.
- Возможный следующий шаг: при истечении/отзыве токена подтвердить, что будущие приватные API используют `get_current_user`, а не только проверку роли в middleware.

## Блокеры
- Нет.