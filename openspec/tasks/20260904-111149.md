# Выполнение INFRA-005 — Модель ScoreResult + миграция

## Дата
2026-09-04

## Что сделано
- Добавлены константы границ метрик psych-портрета (0–10).
- Создана модель `ScoreResult` (SQLAlchemy 2.0) с FK на `applications.id` (ON DELETE CASCADE, индекс), списками позитивных сигналов и факторов риска (JSON), тремя метриками портрета (0–10), отчётом для комитета (Text) + датой обновления, итоговой оценкой (0–100). Добавлена связь один-к-одному с `Application`.
- Написан `alembic` migration `0003` — таблица `score_results` + индекс на `application_id`.
- Добавлены unit-тесты модели.

## Затронутые файлы
- backend/app/core/constants.py
- backend/app/models/score_result.py (новый)
- backend/app/models/application.py (relationship score_result)
- backend/app/models/__init__.py
- backend/alembic/versions/0003_create_score_results_table.py (новый)
- backend/tests/test_score_result.py (новый)
- openspec/TASKS.md — INFRA-005 отмечена выполненной
- openspec/tasks/INFRA-005.md

## Проверка
- pytest: 11 passed (4 — ScoreResult, 4 — Application, 3 — User).
- `alembic upgrade head`: 0002 → 0003 без ошибок; таблица `score_results`, FK и индекс созданы в Postgres.
- `alembic downgrade 0002` + `upgrade head`: roundtrip без ошибок.
- `GET /health`: 200, `database: connected`.

## Что осталось
- Этап 1 завершён (User, Application, ScoreResult). Следующая задача: COMP-001 или Этап 2 (AUTH-001).

## Блокеры
- Нет.
