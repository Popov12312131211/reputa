# Выполнение AUTH-004 — Страница регистрации (frontend `/registration/`)

## Дата
2026-09-04

## Что сделано
- Создан компонент `Registration.jsx` + `Registration.css` по макету `frontend-temp/registration` (карточка, поля, показ/скрытие пароля, живая подсказка требований к паролю, маски телефона и даты, автоподстановка `@` для телеграма).
- Все строки интерфейса вынесены в i18n (namespace `registration` в `ru.json`).
- Константы валидации вынесены в `src/constants/auth.js` (границы пароля, правила, маска телефона, ограничения даты).
- Валидация: ФИО (≥ 3 слов), дата рождения (формат, реальная дата, возраст ≥ 18), логин (обязателен), пароль (8–64, латиница A-z/a-z, цифры, спецсимволы `!@#$_`), повтор пароля (совпадение), телефон (маска `+7(XXX)XXX-XX-XX`), телеграм (`@username`).
- Отправка — заглушка: бэкенд AUTH-001 ещё не готов, показывается сообщение о недоступности (помечено TODO в коде и в TASKS.md).
- Маршрут `/registration` в `App.jsx` переведён с `PlaceholderPage` на `Registration`.

## Затронутые файлы
- frontend/src/constants/auth.js (новый)
- frontend/src/components/Registration.jsx (новый)
- frontend/src/components/Registration.css (новый)
- frontend/src/App.jsx
- frontend/src/locales/ru.json
- openspec/TASKS.md — AUTH-004 отмечена выполненной

## Проверка
- `npm run build`: сборка прошла успешно (vite build, 1873 modules transformed).

## Что осталось
- Подключить реальную отправку на AUTH-001, когда бэкенд будет реализован (в `handleSubmit` заглушка).

## Блокеры
- Зависимость от AUTH-001 (backend) для реальной отправки.
