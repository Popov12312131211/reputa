# COMP-002 — Компонент диаграммы "Благонадёжность" (ScoreGauge, frontend)

## Статус
Выполнено. Спекой закреплено в TASKS.md; расхождений между текстом задачи и реализацией не осталось — единственная правка спекa: в TASKS.md явно дописан диапазон цветовых зон (0–39 / 40–69 / 70–100) и состояние `value = null`.

## Что сделано
- `frontend/src/components/ScoreGauge.jsx` переписан с нуля под COMP-002:
  - один пропс `value` (0–100; `null` — «оценки нет»),
  - палитра не зависит от контекста (собственные CSS-переменные в `.gauge`),
  - i18n-ключ `scoreGauge.title` (заголовок «Благонадёжность (0–100)» над диаграммой),
  - чистая SVG: тонкая подложка 3 зон (rose/amber/green), толстая синяя заливка прогресса, риски+подписи 0/20/40/60/80/100, красная вертикальная риска-маркер, крупное число по центру (цвет по зоне), точка-индикатор под числом.
- `frontend/src/components/ScoreGauge.css` — классы зон/заливки/маркера/рискок/подписей/числа/точки; зоны разделены по границе 40% (низкая) и 70% (высокая) дуги, симметрично границам числа `<40` / `40–69` / `≥70`.
- `frontend/src/components/UserMy.jsx` — вызов заменён на `<ScoreGauge value={detailApp.score} />`; неиспользуемый `userMyDetail.reliability` убран.
- `openspec/TASKS.md` — добавлена/отмечена COMP-002 с полной спецификацией и уточнениями.

## Затронутые файлы
- frontend/src/components/ScoreGauge.jsx (переписан)
- frontend/src/components/ScoreGauge.css (переписан)
- frontend/src/components/UserMy.jsx (вызов компонента)
- frontend/src/locales/ru.json (+scoreGauge.title, −userMyDetail.reliability)
- openspec/TASKS.md (COMP-002 → done)

## Проверка
- Фронтенд-тестов в проекте нет; проверка — `npm run build` в `frontend/` (успешно).

## Решение продукта после приёмки (дополнение)
- Синяя дуга-заливка прогресса **удалена** из компонента (перегружала вид карточек).
- Остаются: статичная цветная 3-зонная шкала (low/mid/high) на всю шкалу 0–100, риска-маркер значения (наклон вдоль радиуса, цвет — как у зоны, в которой лежит значение), риски+подписи, число и точка.
- Прогресс показывается только положением риски-маркера.

## Осталось / передаётся дальше
- EMP-005 будет переиспользовать тот же `ScoreGauge` (одна версия компонента на обе карточки).
- Если в макете (`frontend-temp/Temp.fig`) геометрия делений/дуги отличается — сверка по рис. с этим текстом TASKS.md.

## Блокеры
- Нет.