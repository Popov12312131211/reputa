# Выполнение INFRA-004 — Модель Application + миграция

## Дата
2026-09-04

## Что сделано
- Добавлены константы статусов заявки, лимитов полей и границ оценки скоринга в `app/core/constants.py`.
- Создана модель `Application` (SQLAlchemy 2.0) с FK на `users.id` (ON DELETE CASCADE, индекс), суммой Numeric(12,2), целью, телеграмом, телеграм-каналом, статусом (default `in_queue`) и итоговой оценкой (nullable). Добавлена связь с `User` (relationship `applications`).
- Написан `alembic` migration `0002` — таблица `applications` + индекс на `user_id`.
- Добавлены unit-тесты модели и миграции.

## Затронутые файлы
- backend/app/core/constants.py
- backend/app/models/application.py (новый)
- backend/app/models/user.py (relationship)
- backend/app/models/__init__.py
- backend/alembic/versions/0002_create_applications_table.py (новый)
- backend/tests/test_application.py (новый)
- openspec/TASKS.md — INFRA-004 отмечена выполненной
- openspec/tasks/INFRA-004.md

## Проверка
- pytest: 7 passed (4 — Application, 3 — User).
- `alembic upgrade head`: 0001 → 0002 без ошибок; таблица `applications`, FK и индекс созданы в Postgres.
- `alembic downgrade 0001` + `upgrade head`: roundtrip без ошибок.
- `GET /health`: 200, `database: connected`.

## Что осталось
- Следующая задача: COMP-001 (общий компонент меню/сайдбара) или Этап 2 (AUTH-001 регистрация).

## Блокеры
- Нет.
