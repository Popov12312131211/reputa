# Выполнение INFRA-003 — Модель User + миграция

## Дата
2026-09-04

## Что сделано
- Создана модель `User` (SQLAlchemy 2.0) с полями: ФИО, дата рождения, логин (уникальный), хеш пароля, телефон, телеграм, роль, created_at/updated_at.
- Вынесены константы ролей и лимитов полей в `app/core/constants.py`.
- Инициализирован Alembic и написана начальная миграция `0001` (таблица `users`).
- Добавлены unit-тесты модели и констант.

## Затронутые файлы
- backend/app/core/constants.py (новый)
- backend/app/db/base.py (новый)
- backend/app/models/__init__.py (новый)
- backend/app/models/user.py (новый)
- backend/app/db/session.py (добавлен re-export Base)
- backend/alembic.ini, alembic/env.py, alembic/script.py.mako (новые)
- backend/alembic/versions/0001_create_users_table.py (новый)
- backend/tests/test_user.py (новый)
- openspec/TASKS.md — отметка INFRA-003 выполнена

## Проверка
- pytest: 3 passed.
- Импорт приложения: без ошибок.
- `alembic upgrade head --sql`: миграция генерирует корректный DDL.

## Что осталось
- Применить миграцию к реальному Postgres (`alembic upgrade head`).
- Следующая задача: INFRA-004 (модель Application).

## Блокеры
- Postgres локально не развёрнут; миграции проверялись в offline-режиме.
