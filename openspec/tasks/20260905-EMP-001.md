# EMP-001 — настройки профиля сотрудника

Статус: backend-часть подтверждена.

Сделано:
- переиспользован общий API `GET/PATCH /auth/profile`, доступный авторизованному пользователю независимо от роли;
- добавлен тест изменения профиля сотрудника с проверкой смены пароля и сохранения роли;
- валидация полей, проверка уникальности логина и хеширование пароля остаются общими с APP-006.

Затронутые файлы:
- `backend/tests/test_profile.py`

Осталось:
- frontend `/employee/settings` не входит в текущий запрос и не изменялся.

Проверка:
- `python -m pytest tests/test_profile.py` — 5 passed.
