# CLEAN-001 — Убрать BOM из файлов скоринга

## Дата
2026-09-06

## Статус
Выполнена.

## Что сделано
- Из `backend/app/scoring/stmt_parser.py` и `backend/app/scoring/stmt_scoring.py`
  удалён BOM (U+FEFF, байты `EF BB BF`) в начале файла, оставшийся после
  merge-конфликтов.
- Удалены только 3 байта BOM; остальное содержимое и переносы строк (CRLF)
  сохранены байт-в-байт — `git diff` показывает изменение только первой строки
  каждого файла.
- Проверено, что других файлов с BOM в `backend/app` и `backend/tests` нет.

## Проверка
- `python -m pytest -q` в `backend/` → **179 passed, 43 warnings**.

## Примечания
- Перед прогоном пришлось доустановить `pdfplumber==0.11.10` и `numpy` (зависимости
  `stmt_parser.py`/`stmt_scoring.py`, ранее установленные только локально, ср. APP-007);
  проблем со BOM-удалением не было.

## Блокеры
- Нет.