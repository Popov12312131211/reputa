# AUTH-010 — Backend: регистрация сотрудников с идентификатором

## Статус
Не начато. Задача добавлена в TASKS.md как бэклог, реализация не начиналась.

## Описание
Отдельный эндпоинт регистрации для сотрудников (аналогично AUTH-001, но с ролью employee), с дополнительным обязательным полем "Идентификатор".

Формат идентификатора: одна английская буква + 3 произвольные цифры + literal "room19" (например "A123room19").

Валидация формата — на бэкенде. Сам идентификатор — не секрет доступа сам по себе, а часть проверки принадлежности к команде.

## Принятое решение
**Достаточно проверки формата.** Для MVP список/база допустимых идентификаторов не вводится — регистрация допускает любой идентификатор, прошедший проверку формата (регэксп: `^[A-Za-z][0-9]{3}room19$`). Проверка принадлежности к команде по списку/базе выносится в бэклог, если понадобится ужесточить контроль. Решение принято по аналогии с остальным MVP (регистрация/вход без подтверждения номера/телеграма — см. AUTH-001/AUTH-004).

## Затронутые файлы
- Пока нет (задача не начата).

## Проверка
- Пока не проводилась.

## Что осталось
- Реализация эндпоинта регистрации сотрудника (роль employee) с полем "Идентификатор".
- Валидация формата идентификатора на бэкенде.
- Связанная фронтенд-страница — AUTH-011.

## Блокеры
- Нет.
